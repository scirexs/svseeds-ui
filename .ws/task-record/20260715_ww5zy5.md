# Task: Give DateInput's calendar overlay a default, caller-overridable z-index

Target file: `src/lib/_svseeds/DateInput.svelte`
Test file: `tests/DateInput.svelte.test.ts`
New file (to be created by this task — it does not exist yet, this is not a missing reference):
`.ws/decision/018-dateinput-overlay-default-z-index-via-cssvar.md`

References (read before editing):

- `.ws/policy/coding-style.md` — §13.1 (the `cssvar` custom-property bridge, "direct
  consume" form), §11 (`@component` comment sections), §0 (validation commands).
- `src/lib/_svseeds/DateInput.svelte` — the `overlayStyle` `$derived` already
  implements the identical pattern for the x/y offsets; follow it exactly.
- `.ws/knowledge/browser-style-attribute-serializes-with-spaces.md` — a real browser
  re-serializes `getAttribute("style")` with a space after each colon; assert
  `z-index:\s*var\(...` (regex, as the existing overflow-flip tests already do), never
  the literal authored string.
- `.ws/knowledge/prettier-markdown-exact-body-conflict.md` — Prettier reformats
  hand-authored Markdown; keep `fmt:check` scoped to the changed product files so it
  never walks the new ADR under `.ws/`.

## Goal

`DateInput`'s calendar overlay (`PARTS.BOTTOM`) renders `position:absolute` with no
`z-index`, so it paints at `z-index:auto` and any *later* positioned sibling covers it.
Emit a default `z-index` on the overlay, resolved through the component's existing
`cssvar` bridge, so the calendar sits above adjacent content out of the box while a
caller running their own stacking scheme can retarget or override it.

## Done when

- The open overlay's inline `style` contains `z-index:var(--svs-position-z,1)`.
- `cssvar={{ z: "--my-token" }}` makes the overlay read `var(--my-token,1)` instead.
- The `@component` *Types* block, the `DateInputProps` interface, and the *Behavior*
  section all describe the new `z` key and the default z-index.
- `.ws/decision/018-dateinput-overlay-default-z-index-via-cssvar.md` exists with the
  exact body given in (C-5).
- The Validation commands below all pass.

## Background / constraints

- `.whole` is `position:relative` with `z-index:auto`, which does **not** create a
  stacking context. The overlay therefore competes in the nearest ancestor stacking
  context. The canonical failure this fixes: two stacked `<DateInput>`s — the second's
  relatively-positioned `.whole` paints over the first's open calendar.
- `z-index:1` is deliberate and minimal: `1` beats `auto` (0), which is exactly enough
  to clear the case above, while staying out of the way of a caller's own layers (a
  `z-index:10` sticky header still wins). Do **not** raise it.
- This is a headless library — the default must remain overridable from plain caller
  CSS (`--svs-position-z: 50`) with no prop wiring, and renameable via `cssvar`.
- **Must keep working:** the x/y offset resolution, the `_detectOverflow` per-axis flip
  (`data-svs-flip-x` / `data-svs-flip-y` and the `top`/`bottom`, `left`/`right` swap),
  and the `transition` escape hatch. All 27 existing `DateInput` tests must stay green.

## Scope of changes

- [ ] (C-1) `DateInputCssVar` gains a `"z"` member.
- [ ] (C-2) `overlayStyle` emits the `z-index` declaration via a new `zVar`.
- [ ] (C-3) `@component` docs and the `cssvar` prop comment describe `z`.
- [ ] (C-4) Tests for the default z-index and the `cssvar` rename.
- [ ] (C-5) Create ADR `018-dateinput-overlay-default-z-index-via-cssvar.md`.

## Detailed changes

### (C-1) `DateInputCssVar` gains `"z"`

**Where:** `<script module>`, the `DateInputCssVar` type export.

**Required behavior:** The exported union MUST become `"x" | "y" | "z"`.

```ts
export type DateInputCssVar = "x" | "y" | "z";
```

**Notes:** This widens an already-exported type; no new export name is introduced, so
`src/lib/index.ts` needs no regeneration and `bun run build` is not part of validation.

### (C-2) `overlayStyle` emits the `z-index`

**Where:** the `// *** Reactive Handlers *** //` section — the `xVar` / `yVar` /
`overlayStyle` `$derived` cluster.

**Required behavior:**

- The component MUST resolve the custom-property name with
  `_cssVar(cssvar, "z", "--svs-position-z")` — the same helper and shape as `xVar` /
  `yVar`. It MUST NOT re-derive that lookup inline.
- The overlay's inline style MUST include a `z-index` declaration whose value is
  `var(<resolved name>,1)` — the `var()` fallback `1` (exact), so caller CSS defining
  the property wins and the component needs no `!important`.
- The existing `position`, `top`/`bottom`, and `left`/`right` declarations MUST be
  unchanged.

**Suggested implementation** (illustrative):

```ts
const zVar = $derived(_cssVar(cssvar, "z", "--svs-position-z"));
const overlayStyle = $derived(
  `position:absolute;${overflow.y ? "bottom" : "top"}:var(${yVar},100%);${overflow.x ? "right" : "left"}:var(${xVar},0%);z-index:var(${zVar},1);`,
);
```

**Notes:** `printWidth` is 140 and Prettier does not break template literals, so the
resulting long line is whatever `bun run fmt` produces — run the scoped `fmt:check` and
accept its verdict rather than hand-wrapping.

**Risk:** Low. A positioned element with `z-index:1` now creates a stacking context, so
z-indexes *inside* the overlay become scoped to it. Benign here — the overlay only
contains the `Calendar` (or the caller's `children`) — but it is a real behavior change,
and it is recorded in the ADR's Consequences.

### (C-3) Documentation sync

**Where:** the `@component` comment (*Types* and *Behavior*) and the `DateInputProps`
interface in `<script module>`.

**Required behavior:**

- The `DateInputCssVar` line in the *Types* block MUST match (C-1)'s union.
- The `cssvar` comment MUST be updated in **both** places it appears (the *Types* block
  and the `DateInputProps` interface — the two lines are currently identical and MUST
  stay identical):

  ```ts
  cssvar?: Partial<Record<DateInputCssVar, string>>; // custom-property names for the overlay x/y offsets and z-index; absent key uses default name
  ```

- *Behavior* MUST document the default z-index and how to override it. Keep the existing
  overlay bullet as-is and add one after it, e.g.:

  ```
  - The overlay carries `z-index: var(--svs-position-z, 1)` so it paints above adjacent
    positioned content (e.g. a following `DateInput`'s relatively-positioned root).
    Override it from caller CSS or rename it via the `cssvar` z key to fit a caller's
    own stacking order.
  ```

**Notes:** Prettier does not lint inside the `@component` comment
(`.ws/knowledge/svelte-component-comment-block-skips-prettier.md`), so a passing
`fmt:check` proves nothing here — verify the *Types* block against `DateInputProps` by
hand.

### (C-4) Tests

**Where:** `tests/DateInput.svelte.test.ts`.

**Required behavior:** Add coverage for both the default and the rename. Reuse the
existing `describe("_DateInput overlay overflow flip")` helpers (`overlay`,
`overlayStyle`) or add a sibling `describe` — implementer's choice.

- A test MUST assert that an open overlay's style matches `/z-index:\s*var\(--svs-position-z\s*,\s*1\)/`.
- A test MUST assert that rendering with `cssvar={{ z: "--my-z" }}` yields a style
  matching `/z-index:\s*var\(--my-z/` and **not** containing `--svs-position-z`.
- `expectDefaultAnchor` and the flip tests MUST keep passing unchanged.

**Notes:** `tests/fixtures/DateInputBindable.svelte` spreads `...rest` onto `DateInput`,
so `cssvar` passes straight through — `render(DateInputBindable, { open: true, cssvar: { z: "--my-z" } })`
works without touching the fixture. The overlay is only in the DOM while `open`, and the
existing tests `await tick()` twice after opening (the `observeOverflow` pass) — follow
that pattern.

### (C-5) ADR 018

**Where:** new file `.ws/decision/018-dateinput-overlay-default-z-index-via-cssvar.md`.

**Required behavior:** Create the file with **exactly** this body:

```md
# 018. DateInput overlay default z-index via the cssvar bridge

**Status:** Accepted
**Supersedes:** none
**Date:** 2026-07-15
**Affects:** DateInput public API (`DateInputCssVar`, `cssvar`); the library's convention for absolute-positioned overlays

## Context

`DateInput` renders its calendar overlay (`PARTS.BOTTOM`) as `position:absolute` with no
`z-index`, inside a `.whole` that is `position:relative` with `z-index:auto` — which does
not create a stacking context. The overlay therefore paints in the positioned layer at
`auto`, and any *later* positioned sibling paints over it. Two stacked `<DateInput>`s are
the canonical failure: the second's relatively-positioned root covers the first's open
calendar.

An input calendar is expected to render above adjacent content, so the library should
supply that by default rather than making every caller rediscover the defect.

Two forces constrain the fix:

- The library is headless — it ships no opinionated styles, and `z-index` is effectively
  global. Any value correct for one app is wrong for another, and callers who run a
  deliberate stacking scheme must be able to retarget it without fighting the component.
- A convention for bridging values to CSS custom properties already exists
  (`.ws/policy/coding-style.md` §13.1), and `DateInput` already uses its "direct consume"
  form for the overlay's x/y offsets (`--svs-position-x` / `--svs-position-y`).

## Decision

Emit `z-index: var(--svs-position-z, 1)` on the overlay's inline style, resolving the
custom-property name through the component's existing `cssvar` prop: `DateInputCssVar`
gains a `"z"` key, resolved with `_cssVar(cssvar, "z", "--svs-position-z")`.

The default fallback is `1` — the minimum that fixes the defect. `1` beats `auto` (0),
which is exactly enough to clear a later positioned sibling, while leaving a caller's own
layers alone (a `z-index:10` sticky header still wins).

Callers override at three escalating levels: accept `1`; set `--svs-position-z` from their
own CSS (globally or per instance, no prop wiring); or rename the property via
`cssvar={{ z: "--my-token" }}` to bind their own design token.

## Consequences

- The common stacked-inputs case works with no caller CSS.
- Callers with a stacking scheme get a documented hook and never need `!important` or a
  `styling` override to win.
- `1` and the `--svs-position-z` default name are now public API; changing either later is
  a breaking change for callers relying on the default.
- The overlay now always creates a stacking context (positioned with `z-index` ≠ `auto`),
  so descendant z-indexes are scoped to it. Benign today — the overlay contains only the
  `Calendar` or the caller's `children` — but it is a real behavior change.
- `ComboBox` has the same absolute overlay and the same defect, but exposes no `cssvar`
  prop. It is deliberately out of scope here and should adopt this same shape when
  addressed.

## Rejected alternatives

- **A hard-coded fixed `z-index` (e.g. `10`).** Simplest, and the first thing considered.
  Rejected: a headless library must not preempt the caller's stacking scheme, there is no
  correct universal value, and callers with a layer plan could only fight it with
  `!important`.
- **A dedicated numeric prop `zIndex?: number`.** Explicit and typed. Rejected:
  coding-style §13 routes CSS-value bridging through exactly three surfaces (`cssvar`,
  `duration`, `transition`); a fourth would fragment the convention and duplicate the
  `cssvar` hook the component already exposes for the sibling x/y offsets. It also cannot
  be set from caller CSS, so a global stacking policy would have to be threaded through
  every call site.
- **Do nothing; document that callers style `.bottom` themselves.** Rejected: the
  stacked-`DateInput` failure is a defect, not a styling preference, and every caller
  would rediscover and re-fix it.
- **Move the overlay to the top layer via the native Popover API,** as `Popover` and
  `Toast` do. This sidesteps `z-index` entirely and is the standards-first answer.
  Rejected as out of proportion to a default-styling fix: the overlay is anchored by
  absolute positioning plus the `_detectOverflow` flip logic (`data-svs-flip-x` /
  `data-svs-flip-y`) and a caller-supplied `transition`; the top layer would require CSS
  Anchor Positioning and rework the entire positioning, flip, and transition surface.
  Worth revisiting as its own task.
- **Default `auto`, exposing only the override hook.** Keeps current behavior while
  publishing `--svs-position-z`. Rejected: it does not fix the defect — every caller still
  has to opt in.
```

**Notes:** The body above is the contract (exact). Do not let Prettier reformat it — the
Validation commands are scoped to the changed product files, so `fmt:check` never walks
`.ws/`. See `.ws/knowledge/prettier-markdown-exact-body-conflict.md`.

## Invariants / expected behavior

- With no `cssvar`, the open overlay's style contains `z-index:var(--svs-position-z,1)`.
- With `cssvar={{ z: "--my-z" }}`, it contains `z-index:var(--my-z,1)` and no
  `--svs-position-z`.
- `cssvar={{ x: … }}` / `{{ y: … }}` behavior is unchanged; an absent `z` key falls back
  to the default name, exactly like `x` / `y`.
- The per-axis overflow flip, `data-svs-flip-*`, and the `transition` prop are unaffected.
- SSR-safe: the style is a pure string derivation touching no `window`.

## Out of scope

- `ComboBox` (and every other component) — same defect, but adding it means a new
  `ComboBoxCssVar` type and a new public `cssvar` prop. Deliberately deferred; the ADR
  records it.
- Any move to the native Popover API / top layer for this overlay.
- `_core.ts` — `_cssVar` already does everything needed; it MUST NOT change.
- Any other refactor of `DateInput`, and any change to `Calendar`, `DateField`, or the
  `.ref/` docs.

## Accepted risks / tradeoffs

- `z-index:1` will not lift the overlay above a caller's higher layer (e.g. a
  `z-index:10` header). That is intended — the override hook is the answer, and the
  ADR records the reasoning.
- The default value and default property name become public API, as recorded in the ADR.

## Tests

In `tests/DateInput.svelte.test.ts` (see (C-4)):

- default z-index on the open overlay → `/z-index:\s*var\(--svs-position-z\s*,\s*1\)/`.
- `cssvar` z rename → `/z-index:\s*var\(--my-z/`, and the style does not contain
  `--svs-position-z`.
- Existing 27 tests unchanged and green.

Match the browser-normalized style string with `\s*` regexes, as the existing
overflow-flip tests do
(`.ws/knowledge/browser-style-attribute-serializes-with-spaces.md`).

## Validation

Run from the repo root; all must pass:

- `bun run check`
- `BUN_TEST="tests/DateInput.svelte.test.ts" bun run test` — the 27 existing tests plus
  the new ones.
- `BUN_FMT="./src/lib/_svseeds/DateInput.svelte" BUN_TEST="./tests/DateInput.svelte.test.ts" bun run fmt:check`
  — **scoped on purpose**: it keeps Prettier off the new ADR and the rest of `.ws/`, which
  are workflow artifacts, not product deliverables. Both vars are required
  (`.ws/knowledge/bun-run-fmt-rewrites-whole-src-lib.md`).

`bun run build` is **not** needed — no export name is added (C-1 only widens an existing
exported union).
